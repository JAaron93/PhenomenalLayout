# Development and CI dependencies
# Inherit runtime packages
-r requirements.txt

# Test harness (dev-only)
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-timeout==2.4.0
pytest-cov==7.1.0

# Linters/type-checkers (dev-only)
mypy==2.3.1
ruff==0.16.4

# Optional: richer output and sharding (dev-only)
pytest-xdist==3.8.0
pytest-rerunfailures==16.6
