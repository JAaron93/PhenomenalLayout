# Development and CI dependencies (test harness, linters, and type checkers)
# Install development environment: pip install -r requirements.txt -r requirements-dev.txt

# Test harness (dev-only)
pytest==9.1.1
pytest-asyncio==1.4.0
pytest-timeout==2.3.1
pytest-cov==6.3.0
reportlab>=5.0.1,<6.0

# Linters & type-checkers (dev-only)
mypy==1.20.2
ruff==0.16.5
black==26.5.1

# Optional: richer output and sharding (dev-only)
pytest-xdist==3.6.1
pytest-rerunfailures==14.0
